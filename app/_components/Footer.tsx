import React from "react";

const Footer = () => {
  return (
    <footer className="mt-10 mb-5 px-4 text-center text-xs text-stone-500">
      <p>
        The website is built with Next.js (App router), TypeScript, Tailwind
        CSS, react-hook-form & Resend
      </p>
    </footer>
  );
};

export default Footer;
